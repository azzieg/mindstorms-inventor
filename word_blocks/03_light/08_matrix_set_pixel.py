import hub

# Led on
hub.display.pixel(
    0, # horizontal position, 0 is left edge, 4 is right edge
    4, # vertical position, 0 is top, 4 is bottom
    6) # brightness, 0 is off, 9 is full brightness
