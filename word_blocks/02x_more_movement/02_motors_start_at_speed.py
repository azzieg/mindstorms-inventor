from util.movement import move_start

# Start dual speed
move_start(
    vm,
    [75, -25]) # percent [left, right], left is clockwise for negative values,
               # right is clockwise for positive values
